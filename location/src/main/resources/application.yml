server:
  port: 8081

spring:
  application:
    name: location
  datasource:
      username: 'acode'
      url: jdbc:postgresql://localhost:5432/location
      password: '3316'
  jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: 'true'
      hibernate:
        ddl-auto: update
      show-sql: 'true'